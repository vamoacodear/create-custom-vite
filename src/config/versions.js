const latest = 'latest'

export default {
  eslintVersion: latest,
  eslintReactVersion: latest,
  prettierVersion: latest,
  eslintPrettierVersion: latest,
  eslintPluginPrettierVersion: latest,
  stylelintVersion: latest,
  stylelintConfigStandardVersion: latest,
  stylelintOrderVersion: latest,
  stylelintScssVersion: latest,
  typescriptVersion: latest,
  huskyVersion: '8.0.0',
  lintStagedVersion: '13.0.3',
  testingLibraryReactVersion: latest,
  testingLibraryJestDomVersion: latest,
  testingLibraryUserEventVersion: latest,
  reduxToolkitVersion: latest,
  reactRedux: latest,
  routerVersion: latest,
}
